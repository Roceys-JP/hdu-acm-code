#include<stdio.h>
int main()
{
    int v,d;
    while(~scanf("%d%d",&v,&d))
    {
        int sum = 0 ;
        while(v>d || v != 0)
        {   
            v -= d;
            sum += d;

        }
    }
}
